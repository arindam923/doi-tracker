# Secrets — never commit (env.php at any depth — root + doi-tracker)
env.php
**/env.php
/config.php
/tracking/config.php

# Dumps & backups — never commit live data
backups/
*.sql.gz
export.sql
backup-*.sql

# Local artifacts
storage/logs/*.log
storage/qr_cache/*
storage/geo_cache/*
storage/client_docs/*
.DS_Store
